# BiliFrame

BiliFrame 是面向桌面端哔哩哔哩 HTML5 播放器的 Tampermonkey 用户脚本，为视频观看增加逐帧控制、当前帧截图和原始封面查看功能。

> 本项目主要由 GPT 完成，并经过人工审查。

项目名为 **BiliFrame**，仓库名为 `biliframe`，当前版本为 `0.2.0`。源代码与发行版发布于 [Celesrain/biliframe](https://github.com/Celesrain/biliframe)。

## 功能

- 按浏览器实际解码时间估算帧间隔，逐帧后退或前进；无法取得采样时使用 30 FPS 作为安全回退。
- 将当前视频画面预览并保存为带标题和 BV/剧集标识的 PNG。
- 读取原始封面，预览、打开原图、复制原图地址或下载原图。
- 适配播放器重建、单页导航、宽屏、网页全屏和全屏场景；不满足播放器条件时保持页面不变。
- 控件沿用哔哩哔哩播放器的按钮外观，并提供可访问名称、提示文字和键盘激活。

逐帧操作是浏览器时间线上的近似定位，并非对编码器进行逆向解码；可变帧率视频的步进精度可能不同于恒定帧率视频。

## 支持的路由

脚本匹配以下桌面路由，并且仅在检测到可用视频和播放器控制栏后挂载：

- `https://www.bilibili.com/video/*`
- `https://www.bilibili.com/bangumi/play/*`
- `https://www.bilibili.com/medialist/play/*`
- `https://www.bilibili.com/list/watchlater*`
- `https://www.bilibili.com/festival/*`

移动端页面、直播、嵌入式第三方播放器和离线客户端不在支持范围内。

## 安装与更新

### 从 GitHub 安装

1. 安装并打开兼容 Tampermonkey 的用户脚本管理器。
2. 打开最新 Release 中的 `biliframe.user.js` 附件，或直接打开 [`src/biliframe.user.js`](https://raw.githubusercontent.com/Celesrain/biliframe/main/src/biliframe.user.js)。
3. 检查脚本名称、匹配范围和权限后确认安装。
4. 打开或刷新支持的哔哩哔哩视频页面。

开发者也可将仓库中的 [`src/biliframe.user.js`](src/biliframe.user.js) 拖入管理器，或在管理器中使用“从文件安装”。

脚本文件是可直接安装的单文件 userscript，不需要先构建，也不需要安装运行时依赖。

### 更新

从最新 GitHub Release 或 raw 文件地址重新打开脚本；若管理器提示同名脚本，选择更新现有脚本。脚本的 `@version` 会随发布版本递增。

## 按钮与快捷键

播放器原生播放/暂停按钮后会出现四个按钮，顺序固定为：

| 按钮 | 操作 |
| --- | --- |
| 上一帧 | 暂停视频并向前定位一个估算帧间隔 |
| 下一帧 | 暂停视频并向后定位一个估算帧间隔 |
| 截取当前画面 | 直接单击后按视频原始尺寸截取，并显示可查看、下载的 PNG 预览 |
| 查看视频封面 | 直接单击后显示原始封面，并提供下载、打开原图和复制地址操作 |

快捷键只用于逐帧控制：`Alt+,`（上一帧）和 `Alt+.`（下一帧）。截图与封面按钮不需要 `Alt`，直接单击即可。焦点在输入框、文本域、选择框、按钮、链接或可编辑元素中，或同时按下其他修饰键时，快捷键不会接管页面操作。按钮也可聚焦后使用 `Enter` 或空格键激活。

单击截图或封面按钮只生成并显示预览，不会立即下载或打开新页面。预览使用浏览器原生模态层，因此在普通、网页全屏和浏览器原生全屏下都会位于播放器上方。预览窗口中的“下载截图”“复制当前时间链接”“下载原图”“打开原图”和“复制图片地址”都必须由用户再次主动点击触发；操作结果会直接显示在窗口内。

### 图片文件名模板

可在 Tampermonkey 用户脚本菜单中打开 BiliFrame 的图片名设置，输入保存图片时使用的模板。模板支持以下占位符：

| 占位符 | 含义 |
| --- | --- |
| `{{title}}` | 视频标题 |
| `{{identity}}` | 页面视频标识（BV 号、ep 号等）；默认模板使用此占位符 |
| `{{bvid}}` | 视频 BV 号 |
| `{{timestamp}}` | 当前播放时间（含时分秒） |
| `{{date}}` | 当前日期 |
| `{{time}}` | 当前时间 |
| `{{kind}}` | 图片类型，实际值为 `frame` 或 `cover` |

例如，`{{title}}_{{timestamp}}` 可生成按视频标题和播放时间命名的截图，`{{bvid}}_{{kind}}_{{date}}` 可区分同一视频的截图和封面。扩展名会按图片类型自动追加；Windows 文件名中的非法字符会自动清理。设置保存在 Tampermonkey 的脚本菜单配置中，保存后对当前会话后续下载立即生效，也可以在菜单中恢复默认模板。

## 权限与隐私

脚本声明的权限及用途如下：

- `GM_download`：仅在预览窗口点击“下载截图”或“下载原图”后请求保存图片；远程下载域限制为 `hdslb.com` 及其子域名。
- `GM_openInTab`：仅在点击“打开原图”后以前台标签页打开封面。
- `GM_setClipboard`：仅在点击复制操作后写入图片地址或当前时间链接。
- `GM_getValue` / `GM_setValue`：仅保存一个图片文件名模板字符串。
- `GM_registerMenuCommand` / `GM_unregisterMenuCommand`：仅管理图片文件名设置入口。

BiliFrame 不包含统计、遥测、Cookie、账号数据或后台网络请求；除上述一个本地文件名模板外，不存储用户或账号数据，也不读取私有 Bilibili API、凭据或页面全局私密数据。除用户主动触发的封面打开/下载等行为外，不主动联网。脚本本身无运行时依赖，项目开发检查仅使用 Node.js 内置测试运行器。

## 限制

- 不下载、合并或转码视频/音频流，不绕过 VIP、DRM 或任何访问控制。
- 不修改哔哩哔哩原有快捷键、播放设置或账号状态。
- 浏览器未提供统一的反向解码接口，逐帧后退是基于媒体时间线的定位近似。
- 跨域或受保护的视频画布可能无法导出截图；脚本会显示错误而不是静默失败。
- 没有可用原始封面时，封面按钮无法完成预览；下载器不支持时可改用浏览器打开原图后另存。
- 不自动安装 Tampermonkey 或本脚本。

## 故障排查

### 页面没有出现按钮

确认脚本已启用、页面属于支持路由，并完整刷新视频页。若播放器仍在加载，等待视频控件出现后再检查；直播、移动端或第三方播放器不会挂载。若仍无按钮，请先查看脚本管理器控制台中的错误信息。

### 封面不可用或下载失败

确认页面已加载视频信息且存在封面。可以在封面窗口选择“打开原图”，再由浏览器保存；若管理器拒绝下载，请检查其跨域下载权限以及网络是否能访问 `hdslb.com`。

预览内会显示下载请求、完成或失败原因。Tampermonkey 的“另存为”提示取决于其下载模式和浏览器设置；即使浏览器不显示文件选择窗口，下载仍可能按默认下载目录执行。

### 截图失败

视频必须已经加载并显示有效画面。跨域限制、受保护媒体或浏览器画布安全策略会阻止 PNG 导出；刷新页面并等待播放画面出现仍无效时，属于浏览器安全限制，BiliFrame 不会绕过它。

截图成功后会先显示预览；如果预览已经出现但文件尚未保存，请继续点击预览下方的“下载截图”。

### 逐帧位置不够准确

逐帧间隔来自浏览器已解码帧的采样，刚打开视频、发生缓冲或视频为可变帧率时可能暂时回退或产生近似结果。暂停后连续操作，等待状态提示显示稳定 FPS，可获得更一致的定位。

## 开发检查

本项目不含运行时或构建依赖。需要本地检查时，可使用 Node.js 20 或更高版本运行：

```text
npm run check
npm test
npm run verify
```

验证记录和仍待执行的现场检查见 [`docs/VALIDATION.md`](docs/VALIDATION.md)。
